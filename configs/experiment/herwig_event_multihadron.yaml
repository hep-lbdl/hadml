# @package _global_

# To execute this experiment run:
# python src/train.py experiment=herwig_event_multihadron

defaults:
  - override /datamodule: herwig_event_multihadron.yaml
  - override /model: multihadron_event_gan.yaml

# All parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters
task_name: herwigMultiHadronEvents
tags: ["herwig", "MultiHadronEvents"]

# Set the two flags below to False if you only want to load and preprocess the data, 
# as well as instantiate all the necessary classes. Set both to True to start the training process 
# and invoke testing.
train: False
test: False